////////////////////////////////
marker $$ printstr test $$
////////////////////////////////

memory load atari "${BINARY_PATH}"
symbols load "${SYMBOLS_PATH}"

; Enable print mock to capture OSASCI output
memory write $mock_print_armed 0x(01)

registers flush

run $init_harness until (CP = $end_init_harness OR cycle_count > 500)
assert CP = $end_init_harness $$completed correctly$$

; printstr outputs chars >= 32 via OSASCI — "HELLO" = 48 45 4C 4C 4F
assert #0xC800 ~ "HELLO"   $$printstr output matches "HELLO"$$

assert $_test_result = 0x01     $$printstr completed$$