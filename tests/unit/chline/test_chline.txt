////////////////////////////////
marker $$ chline test $$
////////////////////////////////

memory load atari "${BINARY_PATH}"
symbols load "${SYMBOLS_PATH}"
memory write $mock_print_armed 0x(01)

registers flush

run $init_harness until (CP = $end_init_harness OR cycle_count > 50000)
assert CP = $end_init_harness $$completed correctly$$

; chline sends VDU 25,5,(len&255),(len>>8),0,0,0 then fills with underline char
; cvline sends VDU 25,1,0,0,(len&255),(len>>8),0
; Both use PLOT (VDU 25) sequences and OSWRCH for fill chars
; Just verify they ran — the exact VDU sequence is complex
assert $_test_result = 0x01     $$chline/cvline completed$$