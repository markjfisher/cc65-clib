////////////////////////////////
marker $$ cclearxy test $$
////////////////////////////////

memory load atari "${BINARY_PATH}"
symbols load "${SYMBOLS_PATH}"
memory write $mock_print_armed 0x(01)

registers flush

run $init_harness until (CP = $end_init_harness OR cycle_count > 5000)
assert CP = $end_init_harness $$completed correctly$$

; cclearxy(10,5,20) sends VDU31,x,y via _gotoxy then 20 spaces via cclear
assert #0xC800 = 0x1F     $$VDU 31 from gotoxy$$
assert #0xC801 = 0x05     $$X=5 (left param, pushed first)$$
assert #0xC802 = 0x0A     $$Y=10 (rightmost param in A)$$

; Followed by 20 spaces
assert #0xC803 ~ "                    "  $$20 spaces$$

assert $_test_result = 0x01     $$cclearxy completed$$