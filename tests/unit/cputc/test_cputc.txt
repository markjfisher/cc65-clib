////////////////////////////////
marker $$ cputc test $$
////////////////////////////////

memory load atari "${BINARY_PATH}"
symbols load "${SYMBOLS_PATH}"

; Enable print mock to capture OSWRCH output
memory write $mock_print_armed 0x(01)

registers flush

run $init_harness until (CP = $end_init_harness OR cycle_count > 500)
assert CP = $end_init_harness $$completed correctly$$

; Print mock buffer at $C800 should contain 'A', '!'
assert #0xC800 = 0x41     $$first byte = 'A'$$
assert #0xC801 = 0x21     $$second byte = '!'$$

assert $_test_result = 0x01     $$cputc completed$$