////////////////////////////////
marker $$ preservezp test $$
////////////////////////////////

memory load atari "${BINARY_PATH}"
symbols load "${SYMBOLS_PATH}"
registers flush

run $init_harness until (CP = $end_init_harness OR cycle_count > 5000)
assert CP = $end_init_harness $$completed correctly$$

assert $_test_result + 1 = 0x55  $$ptr1 was clobbered to 0x55 while preserved$$
assert $_test_result = 0xAA      $$restorezp restored ptr1 to 0xAA$$
assert $_test_result + 2 = 0x01  $$completed$$
