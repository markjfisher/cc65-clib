////////////////////////////////
marker $$ file open test $$
////////////////////////////////

memory load atari "${BINARY_PATH}"
symbols load "${SYMBOLS_PATH}"
registers flush

run $init_harness until (CP = $end_init_harness OR cycle_count > 5000)
assert CP = $end_init_harness $$completed correctly$$

assert $_test_result = 0x01     $$open(nonexistent) returned -1$$
assert $_test_result + 2 = 0x01 $$close(invalid fd) returned -1$$