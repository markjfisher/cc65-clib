////////////////////////////////
marker $$ osfind test $$
////////////////////////////////

memory load atari "${BINARY_PATH}"
symbols load "${SYMBOLS_PATH}"

; File mock: hand out handle $11 on a successful open.
memory write #0x0A03 0x(11)

registers flush

run $init_harness until (CP = $end_init_harness OR cycle_count > 2000)
assert CP = $end_init_harness $$completed correctly$$

; The wrapper must return the handle MOS leaves in A ($11), NOT the
; name-pointer high byte. This is the regression for the OSFIND A-vs-Y bug.
assert $_test_result = 0x11      $$osfind returned the MOS handle in A$$
assert $_test_result + 1 = 0x00  $$osfind cleared the return high byte$$
